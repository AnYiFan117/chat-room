# Vite 环境变量示例，复制为 .env 并替换为实际服务信息
# 若有多个信令端点，可用逗号分隔或直接提供 JSON 数组；room 参数会由代码自动拼接
# 例如：VITE_SIGNALING_ENDPOINTS=["wss://signal-1.example/ws","wss://signal-2.example/ws"]
VITE_SIGNALING_ENDPOINTS=wss://your-signaling-service.example/signal

# 以 JSON 数组的形式提供 ICE 服务器配置
# 需要 TURN 服务时填写用户名与密码，若仅使用 STUN 可省略
VITE_ICE_SERVERS=[{"urls":"stun:your-stun.example:3478"},{"urls":"turn:your-turn.example:3478?transport=tcp","username":"turn-user","credential":"turn-password"}]
